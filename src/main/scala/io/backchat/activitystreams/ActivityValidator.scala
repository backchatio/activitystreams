package io.backchat.activitystreams

//import com.codahale.jerkson.AST._

class ActivityValidator {

//  def validateTrackerActivity(json: String): Either[Seq[String], JValue] = Right(JNull)
//
//  def validateIndexerActivity(json: String): Either[Seq[String], JValue] = Right(JNull)
}
